# Blog_Vercel - Claude Code Context

This project is configured for **Claude Code on the web**.

## Repository Instructions
- **Configuration**: Claude reads this file to understand the project structure and rules. Add any project-specific guidelines or environment needs here.
- **Dependency Management**: By default, `.claude/settings.json` configures a `SessionStart` hook that runs `scripts/install_pkgs.sh`. Use that script to install packages (e.g., `npm install`, `pip install`) when a remote session starts.

## Useful Commands
- (Add your build/run/test commands here as the project grows)
